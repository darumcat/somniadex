{
  "name": "somniadex",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && netlify deploy --prod"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "vite": "^4.5.11",
    "@vitejs/plugin-basic-ssl": "^1.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
